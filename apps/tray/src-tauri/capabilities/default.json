{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Coriven tray app.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["sign-in"],
  "permissions": [
    "opener:default",
    "core:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
