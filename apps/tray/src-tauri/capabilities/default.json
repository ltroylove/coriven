{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Coriven tray app (tray-only, no windows).",
  "platforms": ["linux", "macOS", "windows"],
  "windows": [],
  "permissions": [
    "opener:default"
  ]
}
